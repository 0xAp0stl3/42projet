# inception

Projet Docker : automatisation du déploiement de services (WordPress, MariaDB, Nginx, etc.) via docker-compose.

## Lancement

```bash
docker-compose up --build
```

## Structure

- `srcs/` : fichiers de configuration et Dockerfiles